import { defaultState } from '../../server/defaultState';
// /import { createTask } from '../actions/createTask';


export const usersReducer = (users=defaultState, action) => {
  
    return users;
} 